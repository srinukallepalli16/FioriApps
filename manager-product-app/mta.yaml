_schema-version: 3.3.0
ID: manager-product-app
description: A simple CAP project.
version: 1.0.0
modules:
- name: manager-product-app-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: manager-product-app-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: manager-product-app-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: manager-product-app-db
  parameters:
    buildpack: nodejs_buildpack
- name: manager-product-app-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: manager-product-app-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comappmanagerui.zip
      name: comappmanagerui
      target-path: resources/
- name: comappmanagerui
  type: html5
  path: app/managerui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: manager-product-app-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: manager-product-app-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: manager-product-app-html5-service
    service-plan: app-host
- name: manager-product-app-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: manager-product-app-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
