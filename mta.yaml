_schema-version: 3.3.0
ID: CAPM_03
description: A simple CAP project.
version: 1.0.0
modules:
- name: CAPM_03-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: CAPM_03-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: CAPM_03-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: CAPM_03-db
  parameters:
    buildpack: nodejs_buildpack
- name: CAPM_03-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CAPM_03-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - bookshop.zip
      name: bookshop
      target-path: resources/
- name: bookshop
  type: html5
  path: app/bookshop
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CAPM_03-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: CAPM_03-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CAPM_03-html5-service
    service-plan: app-host
- name: CAPM_03-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: CAPM_03-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
